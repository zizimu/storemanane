<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.rainbow.mapper.TbPreactMapper" >

    <resultMap id="BaseResultMap" type="org.rainbow.pojo.TbPreact" >
        <id column="pid" property="pid" jdbcType="INTEGER" />
        <result column="pname" property="pname" jdbcType="VARCHAR" />
        <result column="pact" property="pact" jdbcType="VARCHAR" />
        <result column="mark" property="mark" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        pid, pname, pact, mark
    </sql>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
        select 
        <include refid="Base_Column_List" />
        from tb_preAct
        where pid = #{pid,jdbcType=INTEGER}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
        delete from tb_preAct
        where pid = #{pid,jdbcType=INTEGER}
    </delete>

    <insert id="insert" parameterType="org.rainbow.pojo.TbPreact" >
        insert into tb_preAct (pid, pname, pact, 
            mark)
        values (#{pid,jdbcType=INTEGER}, #{pname,jdbcType=VARCHAR}, #{pact,jdbcType=VARCHAR}, 
            #{mark,jdbcType=VARCHAR})
    </insert>

    <insert id="insertSelective" parameterType="org.rainbow.pojo.TbPreact" >
        insert into tb_preAct
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="pid != null" >
                pid,
            </if>
            <if test="pname != null" >
                pname,
            </if>
            <if test="pact != null" >
                pact,
            </if>
            <if test="mark != null" >
                mark,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="pid != null" >
                #{pid,jdbcType=INTEGER},
            </if>
            <if test="pname != null" >
                #{pname,jdbcType=VARCHAR},
            </if>
            <if test="pact != null" >
                #{pact,jdbcType=VARCHAR},
            </if>
            <if test="mark != null" >
                #{mark,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="org.rainbow.pojo.TbPreact" >
        update tb_preAct
        <set >
            <if test="pname != null" >
                pname = #{pname,jdbcType=VARCHAR},
            </if>
            <if test="pact != null" >
                pact = #{pact,jdbcType=VARCHAR},
            </if>
            <if test="mark != null" >
                mark = #{mark,jdbcType=VARCHAR},
            </if>
        </set>
        where pid = #{pid,jdbcType=INTEGER}
    </update>

    <update id="updateByPrimaryKey" parameterType="org.rainbow.pojo.TbPreact" >
        update tb_preAct
        set pname = #{pname,jdbcType=VARCHAR},
            pact = #{pact,jdbcType=VARCHAR},
            mark = #{mark,jdbcType=VARCHAR}
        where pid = #{pid,jdbcType=INTEGER}
    </update>
</mapper>